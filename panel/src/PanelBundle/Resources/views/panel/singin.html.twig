{% extends 'base.html.twig' %}

{% block body %}
    {% for flashMessage in app.session.flashBag.get('error') %}
        <div class="fixed">
            <span class="alerta">
                {{ flashMessage }}
            </span>
        </div>
    {% endfor %}
    {{ form_start(form) }}
    {{ form_end(form) }}
{% endblock %}